## 2026-05-31 - Expensive Deep Cloning in React State Updates\n**Learning:** In a React Native game loop, using `JSON.parse(JSON.stringify(state))` for state updates inside an animation loop that triggers every 200ms causes significant garbage collection overhead and potential UI jank. \n**Action:** Use targeted shallow cloning using spread operators instead, especially when updating state in fast loops.
